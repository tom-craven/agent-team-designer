# Architecture

C4 L1–L2 plus dependency rules. Keep L3/L4 in type and module nodes.

## System context
Who uses the system. Which external systems it talks to.

## Containers
Services, data stores, queues. One short paragraph each. Link owning contexts.

## Dependency direction
Allowed arrows. The hard no's. These are constraints, not a snapshot of today's imports.

## Consistency boundaries
Where transactions, outboxes, or eventual consistency apply.

## Load-bearing decisions
- `decision:0001` — title
- `decision:0002` — title

## See also
- `knowledge/system.md`
- `knowledge/contexts/`
