# Module name

Bounded context: `context:name`
Load first: `knowledge/contexts/name.md`

## Public types
- `TypeA` — one-line job (`TypeA.context.md`)
- `TypeB` — one-line job (`TypeB.context.md`)

## Dependency direction
This module may depend on: …
This module must not import: …

## Read before changing
- Hard invariants that apply in this package
- `knowledge/decisions/…` if a choice is still load-bearing

## Commands
```bash
# test / lint commands for this package only
```
