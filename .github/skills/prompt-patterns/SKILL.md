---
name: prompt-patterns
description: Apply concise, role-specific system-prompt patterns when creating, rewriting, or standardising agent prompts.
---

# Prompt Patterns

The canonical skill is
`.opencode/skills/prompt-patterns/SKILL.md`. Read that repository file and follow
its complete workflow. This adapter exists only to expose the canonical
first-party skill through Copilot CLI's repository skill discovery; edit the
canonical file, not this adapter, when changing the workflow.
